from gpiozero import LED, PWMOutputDevice
from signal import pause
#set frequency
led = PWMOutputDevice(pin=17, active_high=True, initial_value=25, frequency=200):

led.value = 0.25 #Set 25% duty cycle











def pulse(self, fade_in_time = 3, fade_out_time = 3)
	self.time
	(
		fade_in_time, fade_out_time
	)
pause()
